# Account Switching Flow Diagram

## Visual Flow Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     PARENT LOGIN                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Enter credentials                                      │  │
│  │ 2. authStore.signIn()                                     │  │
│  │ 3. Navigate to /parent-dashboard                          │  │
│  │ 4. accountSwitchStore.setActiveAccount('parent')          │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                   PARENT DASHBOARD                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • View children list                                      │  │
│  │ • See analytics and activity                              │  │
│  │ • Click "Switch to [Child Name]"                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│              SWITCH TO CHILD (handleSwitchToChildView)           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Save parent credentials → localStorage.parent_session │  │
│  │ 2. Get child tokens from backend                          │  │
│  │ 3. Update localStorage.access_token (child's token)       │  │
│  │ 4. Update localStorage.user_data (child's data)           │  │
│  │ 5. accountSwitchStore.setActiveAccount('child', id, name) │  │
│  │ 6. Navigate to /home                                      │  │
│  │ 7. window.location.reload() - refresh auth state          │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                      CHILD VIEW                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • Home page shows child's perspective                     │  │
│  │ • Can create stories, view library, etc.                  │  │
│  │ • Settings shows "Back to Parent Dashboard" button        │  │
│  │ • ❌ Cannot access /parent-dashboard (ParentRoute blocks) │  │
│  │ • ❌ Cannot access /parent-settings (ParentRoute blocks)  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
            ┌───────────────────────────────┐
            │   USER CLOSES APP             │
            │   (Browser closed/refreshed)  │
            └───────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                    APP REOPENS                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Zustand loads persisted state from localStorage        │  │
│  │ 2. accountSwitchStore.activeAccountType = 'child'         │  │
│  │ 3. authStore loads child's user data                      │  │
│  │ 4. Still in CHILD VIEW ✅                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
            ┌───────────────────────────────┐
            │   CHILD TRIES TO ACCESS       │
            │   /parent-dashboard           │
            └───────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                  PARENTROUTE PROTECTION                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Check: isAuthenticated? ✓                             │  │
│  │ 2. Check: user is parent/teacher? ✓                       │  │
│  │ 3. Check: activeAccountType === 'child'? ✓               │  │
│  │ 4. ❌ BLOCKED! Redirect to /home                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
            ┌───────────────────────────────┐
            │   GO TO SETTINGS              │
            │   Click "Back to Parent"      │
            └───────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│              PARENT PASSWORD VERIFICATION                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Show ParentPasswordVerificationModal                   │  │
│  │ 2. User enters parent password                            │  │
│  │ 3. Backend verifies password                              │  │
│  │ 4. ✓ Password correct                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│            SWITCH BACK TO PARENT (handleParentPasswordVerified)  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Retrieve parent_session from localStorage              │  │
│  │ 2. Restore parent's access_token                          │  │
│  │ 3. Restore parent's refresh_token                         │  │
│  │ 4. Restore parent's user_data                             │  │
│  │ 5. Remove parent_session from localStorage                │  │
│  │ 6. accountSwitchStore.setActiveAccount('parent')          │  │
│  │ 7. window.location.href = '/parent-dashboard'             │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                 BACK TO PARENT DASHBOARD                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • Full parent access restored ✓                           │  │
│  │ • Can access /parent-dashboard ✓                          │  │
│  │ • Can manage children ✓                                   │  │
│  │ • activeAccountType = 'parent' persists ✓                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## State Transitions

```
┌──────────────┐
│  NOT LOGGED  │
│     IN       │
└──────┬───────┘
       │ Login as Parent
       ↓
┌──────────────┐      Switch to Child      ┌──────────────┐
│   PARENT     │─────────────────────────→ │    CHILD     │
│    MODE      │                            │    MODE      │
│              │←───────────────────────────│              │
└──────────────┘   Password + Switch Back   └──────────────┘
       │                                           │
       │ Sign Out                                  │ Sign Out
       ↓                                           ↓
┌──────────────┐                            ┌──────────────┐
│  NOT LOGGED  │                            │  NOT LOGGED  │
│     IN       │                            │     IN       │
└──────────────┘                            └──────────────┘
```

---

## localStorage State During Flow

### State 1: Parent Mode
```javascript
{
  "access_token": "parent_access_token_xyz",
  "refresh_token": "parent_refresh_token_abc",
  "user_data": {
    "id": "123",
    "name": "John Doe",
    "user_type": "parent",
    ...
  },
  "account-switch-storage": {
    "state": {
      "activeAccountType": "parent",
      "activeChildId": null,
      "activeChildName": null
    }
  }
  // No parent_session
}
```

### State 2: Child Mode (After Switch)
```javascript
{
  "access_token": "child_access_token_xyz",  // ← Changed
  "refresh_token": "child_refresh_token_abc",  // ← Changed
  "user_data": {  // ← Changed to child's data
    "id": "456",
    "name": "Emma Doe",
    "user_type": "child",
    ...
  },
  "account-switch-storage": {  // ← Updated
    "state": {
      "activeAccountType": "child",
      "activeChildId": 456,
      "activeChildName": "Emma Doe"
    }
  },
  "parent_session": {  // ← NEW: Parent credentials saved
    "id": 123,
    "name": "John Doe",
    "tokens": {
      "access": "parent_access_token_xyz",
      "refresh": "parent_refresh_token_abc"
    },
    "userData": {...parent user data...}
  }
}
```

### State 3: Back to Parent Mode
```javascript
{
  "access_token": "parent_access_token_xyz",  // ← Restored
  "refresh_token": "parent_refresh_token_abc",  // ← Restored
  "user_data": {  // ← Restored to parent's data
    "id": "123",
    "name": "John Doe",
    "user_type": "parent",
    ...
  },
  "account-switch-storage": {  // ← Updated
    "state": {
      "activeAccountType": "parent",
      "activeChildId": null,
      "activeChildName": null
    }
  }
  // parent_session REMOVED
}
```

---

## Security Checkpoints

```
┌─────────────────────────────────────────────────────────┐
│                    Route Access Request                  │
│                  (e.g., /parent-dashboard)               │
└────────────────────────┬────────────────────────────────┘
                         │
                         ↓
              ┌──────────────────────┐
              │  ParentRoute Check   │
              └──────────┬───────────┘
                         │
          ┌──────────────┼──────────────┐
          ↓              ↓              ↓
    ┌─────────┐   ┌─────────┐   ┌─────────────┐
    │ Auth?   │   │ Parent/ │   │ Viewing as  │
    │         │   │ Teacher?│   │   Child?    │
    └────┬────┘   └────┬────┘   └──────┬──────┘
         │             │               │
         │✓            │✓              │
         ↓             ↓               ↓
    ┌─────────────────────────────────────┐
    │         ALL CHECKS PASS?             │
    └────────────┬────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
        ↓                 ↓
    ✅ ALLOW         ❌ REDIRECT
    Access to         to /home
    parent route
```

---

## Component Responsibilities

```
┌─────────────────────────────────────────────────────────────┐
│                    accountSwitchStore                        │
│  • Persists activeAccountType to localStorage               │
│  • Provides setActiveAccount() and clearActiveAccount()     │
│  • Survives app restarts via Zustand persist                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      ParentRoute                             │
│  • Wraps /parent-dashboard and /parent-settings routes      │
│  • Checks authentication status                             │
│  • Verifies parent/teacher privileges                       │
│  • Blocks access if activeAccountType === 'child'           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  ParentDashboardPage                         │
│  • Sets activeAccountType = 'parent' on mount               │
│  • Calls switchToChildView() when parent switches           │
│  • Updates activeAccountType = 'child' during switch        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                       HomePage                               │
│  • Checks user type on mount                                │
│  • Sets activeAccountType appropriately                     │
│  • Handles both direct login and parent-as-child cases      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                     SettingsPage                             │
│  • Shows "Back to Parent" only when parent_session exists   │
│  • Triggers ParentPasswordVerificationModal                 │
│  • Restores parent state after verification                 │
│  • Updates activeAccountType = 'parent' on switch back      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      authStore                               │
│  • Clears accountSwitchStore on sign out                    │
│  • Removes parent_session on sign out                       │
│  • Ensures clean state for next login                       │
└─────────────────────────────────────────────────────────────┘
```

---

## Timeline Example

```
Time  │ Action                        │ activeAccountType │ Can Access Parent Routes
──────┼───────────────────────────────┼───────────────────┼──────────────────────────
09:00 │ Parent logs in                │ 'parent'          │ ✅ Yes
09:05 │ Parent switches to child      │ 'child'           │ ❌ No
09:10 │ Child uses app                │ 'child'           │ ❌ No
09:15 │ Browser closed                │ 'child'           │ ❌ No
      │                               │ (persisted)       │
15:00 │ Browser reopened              │ 'child'           │ ❌ No
15:05 │ Still viewing as child        │ 'child'           │ ❌ No
15:10 │ Try /parent-dashboard         │ 'child'           │ ❌ No (redirected)
15:15 │ "Back to Parent" + password   │ 'parent'          │ ✅ Yes
15:20 │ Parent dashboard accessible   │ 'parent'          │ ✅ Yes
```

---

## Key Insights

1. **Persistent by Design**: `activeAccountType` survives browser restarts
2. **Multi-Layer Security**: Protection at route, state, and auth levels
3. **Password Gated**: Switching back requires parent password
4. **Fail-Safe**: Default behavior is to block, not allow
5. **Clear State**: Always know which account mode is active

This ensures children cannot accidentally (or intentionally) access parent features! 🔒
